/// Stable identifiers used to reconcile one Cursor native approval decision.
struct CursorNativeAttentionIdentifiers {
    let scopeId: String
    let observationId: String
    let expectedToolCallId: String?
}
