import AppKit

/// Shared layout constants for the native file-preview text editor.
enum FilePreviewTextEditorLayout {
    static let textContainerInset = NSSize(width: 12, height: 10)
    static let lineFragmentPadding: CGFloat = 0
}
